import { INewUser } from "@/types/index.js";

export async function createUserAccount(user: INewUser) {
    try {
        const new
    } catch (error) {
        console.log(error);
        return error;        
    }
}